{
  "name": "lightweight-cicd-test-app",
  "version": "1.0.0",
  "private": true,
  "description": "Root package that lets the CI/CD pipeline lint, build and test both workspaces with plain npm commands.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "lint": "npm run lint --workspace backend",
    "build": "npm run build --workspace frontend",
    "test": "npm run test --workspace backend"
  }
}
